// searching pseudocode????
// recipes.map(recipe => {
//     if (searchedIngredients.every(val => recipe.ingredients.includes(val))) {
//       return recipe;
//     }
//   })

// search bar goes here
